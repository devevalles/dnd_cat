Executa aquesta comanda en un terminal en segon pla per arrencar el servidor local:

```
python3 -m http.server 8000
```

Després dis-li a l'usuari que obri el navegador a http://localhost:8000 per veure la pàgina.
